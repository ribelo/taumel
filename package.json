{
  "name": "taumel",
  "private": true,
  "type": "module",
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "ocaml:init": "nix develop -c taumel-opam-init",
    "build:ocaml": "nix develop -c sh scripts/build-ocaml.sh",
    "test:ocaml": "nix develop -c dune runtest",
    "smoke:artifact": "nix develop -c node test/smoke_artifact.mjs",
    "smoke:entrypoint": "bun test/smoke_entrypoint.mjs",
    "gate": "npm run build:ocaml && npm run test:ocaml && npm run smoke:artifact && npm run smoke:entrypoint"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.9",
    "@earendil-works/pi-tui": "^0.79.9"
  }
}
